import styled from "@emotion/styled";
import { theme } from "constants";
export const FeedbackStats = styled.ul`
list-style: none;
font-size: ${theme.sizing(5)};
`
export const StatsItem = styled.li`

`;
export const StatsValue = styled.span`

`;